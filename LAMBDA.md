# Rust クロージャ完全理解ガイド (Rust Closures Deep Dive: Fn, FnMut, FnOnce)

Rust における無名関数（ラムダ式）である **「クロージャ（Closures）」** の完全解説書です。

Java, C#, Go, Python などのエンジニアが必ず直面する「`Fn`, `FnMut`, `FnOnce` の 3 つのトレイトの違い」「裏で生成される無名構造体とヒープ確保ゼロの仕組み」「`move` キーワードの使い分け」「関数ポインタ `fn` との違い」まで徹底的に解説します。

---

## 📑 目次

1. [クロージャの基本構文と特徴](#1-クロージャの基本構文と特徴)
2. [キャプチャの 3 モード（借用・可変借用・ムーブ）](#2-キャプチャの-3-モード借用可変借用ムーブ)
3. [クロージャトレイトの 3 兄弟 (`FnOnce`, `FnMut`, `Fn`) ★最重要★](#3-クロージャトレイトの-3-兄弟-fnonce-fnmut-fn-最重要)
4. [コンパイラの内部実装：無名構造体とゼロコスト抽象化](#4-コンパイラの内部実装無名構造体とゼロコスト抽象化)
5. [高階関数での受け取りと戻り値 (`impl Fn` vs `Box<dyn Fn>`)](#5-高階関数での受け取りと戻り値-impl-fn-vs-boxdyn-fn)
6. [関数ポインタ (`fn`) との違い](#6-関数ポインタ-fn-との違い)
7. [他言語エンジニア向け比較表 (Rust vs C# vs Java vs Modern C++ vs Go vs Python)](#7-他言語エンジニア向け比較表-rust-vs-c-vs-java-vs-modern-c-vs-go-vs-python)
8. [理解度チェッククイズ & よくあるエラー虎の巻](#8-理解度チェッククイズ--よくあるエラー虎の巻)

---

## 1. クロージャの基本構文と特徴

Rust のクロージャはパイプ記号 `|...|` で引数を囲みます。

```rust
// 1. 引数と戻り値の型を明記した場合
let add_one = |x: u32| -> u32 { x + 1 };

// 2. 型推論と中括弧の省略（単一式の場合）
let add_one_short = |x| x + 1;

// 3. 引数なし
let print_hello = || println!("Hello, world!");
```

### 💡 型の厳格な固定化
クロージャは最初に呼び出されたときの引数型で**型が完全に固定（Lock）**されます。
```rust
let identity = |x| x;
let s = identity(String::from("hello")); // ここで identity は String -> String に決定
// let n = identity(5); // ❌ コンパイルエラー: 期待される型は String
```

---

## 2. キャプチャの 3 モード（借用・可変借用・ムーブ）

クロージャが外側の変数を使用するとき、コンパイラは**「必要最小限の権限」**を自動選択してキャプチャします。

| キャプチャ方法 | キャプチャ時の権利 | 外部変数の状態 |
| :--- | :--- | :--- |
| **不変借用 (`&T`)** | 読み取り専用 | 外側からも引き続き読み取り可能 |
| **可変借用 (`&mut T`)** | 読み書き可能 | クロージャが生存中は外側からアクセス不可 |
| **ムーブ (`move`)** | 完全な所有権 | 外側の変数は使用不能（破棄・移譲） |

```rust
// 1. 不変借用 (&T)
let list = vec![1, 2, 3];
let print_list = || println!("List: {:?}", list);
print_list();
println!("Outer list still accessible: {:?}", list); // ⭕ OK

// 2. 可変借用 (&mut T)
let mut count = 0;
let mut inc = || count += 1;
inc();
// println!("{}", count); // ❌ inc の可変借用が生きている間は外側から参照不可
println!("Count: {}", count); // inc の使用終了後なら OK

// 3. move による強制所有権移譲
let data = vec![10, 20];
let consume = move || println!("Data len: {}", data.len());
consume();
// println!("{:?}", data); // ❌ コンパイルエラー: data はクロージャ内にムーブ済み
```

---

## 3. クロージャトレイトの 3 兄弟 (`FnOnce`, `FnMut`, `Fn`) ★最重要★

Rust では、すべてのクロージャは以下の 3 つのトレイトのいずれか（または複数）を自動的に実装します。

### 3-1. トレイトの継承階層
```
       ┌───────────┐
       │  FnOnce   │ (最上位: 1回は必ず呼べる)
       └─────▲─────┘
             │
       ┌─────┴─────┐
       │   FnMut   │ (FnOnce を継承: 内部状態を変更しながら複数回呼べる)
       └─────▲─────┘
             │
       ┌─────┴─────┐
       │    Fn     │ (FnMut を継承: 内部状態を変更せず複数回呼べる)
       └───────────┘
```

- `Fn` を実装しているクロージャは、自動的に `FnMut` と `FnOnce` も満たします。
- `FnMut` を実装しているクロージャは、自動的に `FnOnce` を満たします。

### 3-2. 各トレイトのシグネチャと挙動

| トレイト | `call` メソッドのシグネチャ | キャプチャ環境の扱い | 呼び出し可能回数 | 主な用途 |
| :--- | :--- | :--- | :--- | :--- |
| **`FnOnce`** | `fn call_once(self, args)` | **`self`（所有権）を消費** | **1回のみ** | 資源の破棄、スレッド起動 (`thread::spawn`) |
| **`FnMut`** | `fn call_mut(&mut self, args)` | **`&mut self`（可変参照）** | **複数回** | カウンタ増加、内部ベクタの変更 |
| **`Fn`** | `fn call(&self, args)` | **`&self`（不変参照）** | **複数回** | 読み取り専用計算、並行処理 |

---

## 4. コンパイラの内部実装：無名構造体とゼロコスト抽象化

C# や Java では、クロージャが変数をキャプチャするとヒープ上にクラス（`DisplayClass` 等）が `new` され、GC プレッシャーが発生します。

しかし Rust では、**コンパイラがスタック上に専用の無名構造体（Anonymous Struct）を直接生成**します。

```rust
// 私たちが書いたコード:
let factor = 3;
let multiply = |x: i32| x * factor;
```

コンパイル時、Rust コンパイラは裏で以下のような構造体を生成しています：

```rust
// コンパイラが裏で生成する内部構造 (概念コード):
struct ClosureEnvironment<'a> {
    factor: &'a i32, // キャプチャした変数の参照をフィールドに持つ構造体
}

impl<'a> Fn<(i32,)> for ClosureEnvironment<'a> {
    extern "rust-call" fn call(&self, args: (i32,)) -> i32 {
        args.0 * (*self.factor)
    }
}
```

> **🚀 ゼロコスト抽象化**:  
> キャプチャする変数の参照（ポインタ）だけを格納した構造体がスタックに作られ、呼び出しは通常の構造体メソッドとしてインライン展開されます。**ヒープアロケーションも GC も一切発生しません**。

---

## 5. 高階関数での受け取りと戻り値 (`impl Fn` vs `Box<dyn Fn>`)

### 5-1. 引数としてクロージャを受け取る（静的ディスパッチ）
```rust
// ジェネリクスとトレイト境界 (推奨: インライン展開されて最速)
fn run_operation<F>(op: F) 
where 
    F: Fn(i32) -> i32 
{
    println!("Result: {}", op(10));
}

// 簡易構文 impl Trait
fn run_operation_short(op: impl Fn(i32) -> i32) {
    println!("Result: {}", op(10));
}
```

### 5-2. 戻り値としてクロージャを返す
```rust
// 1. 静的ディスパッチ (単一のクロージャを返す場合: アロケーションゼロ)
fn make_multiplier(factor: i32) -> impl Fn(i32) -> i32 {
    move |x| x * factor // move が必須 (factor の所有権をクロージャに閉じ込める)
}

// 2. 動的ディスパッチ (条件分岐で異なるクロージャを返す場合: Box によるヒープ確保)
fn make_operator(is_add: bool) -> Box<dyn Fn(i32, i32) -> i32> {
    if is_add {
        Box::new(|a, b| a + b)
    } else {
        Box::new(|a, b| a * b)
    }
}
```

---

## 6. 関数ポインタ (`fn`) との違い

Rust には、クロージャトレイト（大文字 `Fn`）とは別に、小文字の **関数ポインタ型 (`fn`)** が存在します。

```rust
// 通常の関数
fn square(x: i32) -> i32 { x * x }

// 関数ポインタ fn を受け取る関数
fn apply_fn(f: fn(i32) -> i32, val: i32) -> i32 {
    f(val)
}
```

| 比較項目 | 関数ポインタ (`fn`) | クロージャ (`Fn / FnMut / FnOnce`) |
| :--- | :--- | :--- |
| **環境キャプチャ** | ❌ **一切キャプチャできない** | ⭕ **外部変数をキャプチャ可能** |
| **型の種類** | プリミティブな型ポインタ | コンパイラ固有の無名構造体型 |
| **サイズ** | 常にポインタ 1 個分 (8 バイト) | キャプチャする変数によって変動 (ZST〜) |
| **互換性** | キャプチャなしのクロージャなら `fn` に暗黙変換可能 | `fn` から `Fn` への変換は自動 |

---

## 7. 他言語エンジニア向け比較表 (Rust vs C# vs Java vs Modern C++ vs Go vs Python)

| 項目 | Rust | C# (12+) | Java (21+) | Modern C++ (20+) | Go | Python |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **基本構文** | `\|x\| x * 2` | `x => x * 2` | `x -> x * 2` | `[](auto x){ return x*2; }` | `func(x int) int` | `lambda x: x*2` |
| **メモリ配置** | **スタック (無名構造体)**| ヒープ (`DisplayClass`) | ヒープ (`invokedynamic`) | スタック (無名ファンクタ) | ヒープ (エスケープ解析) | ヒープ (`function`) |
| **GC / 解放** | **GC なし (スコープ破棄)**| GC (Gen 0/1/2) | GC | RAII (デストラクタ) | GC | 参照カウント + GC |
| **所有権移動** | ⭕ **`move`** | ❌ 参照コピー | ❌ 参照コピー | ⭕ `[x = std::move(x)]` | ❌ ポインタ参照 | ❌ 参照コピー |
| **状態変更** | ⭕ `FnMut` (型安全) | ⭕ 自由に変更可能 | ❌ 不可 (final 限定) | ⭕ `mutable` 指定 | ⭕ 自由に変更可能 | ⚠️ `nonlocal` |
| **呼出回数制約** | ⭕ **`FnOnce` で保証** | ❌ なし (実行時例外) | ❌ なし | ❌ なし (自己責任) | ❌ なし | ❌ なし |

---

## 8. 理解度チェッククイズ & よくあるエラー虎の巻

### Q1. 次のコードがコンパイルエラーになる理由は何ですか？
```rust
fn test() {
    let s = String::from("hello");
    let c = || drop(s);
    c();
    c(); // 2回目の呼び出し
}
```
<details>
<summary>▶ 解答と解説</summary>

**解答**: `drop(s)` により環境変数 `s` の所有権が消費されるため、クロージャ `c` は **`FnOnce`** のみを実装します。`FnOnce` は `call_once(self)` で自身を消費するため、2 回以上呼び出すことはできません。コンパイラは `use of moved value: c` エラーを出力します。
</details>

### Q2. スレッド `std::thread::spawn` にクロージャを渡す際、なぜ `move` キーワードが必要になることが多いのですか？
<details>
<summary>▶ 解答と解説</summary>

**解答**: `thread::spawn` は新しく起動するスレッドが親スレッドよりも長く生存する可能性があるため、引数のクロージャに `'static` ライフタイムを要求します。`move` を付けないと親スタックのローカル変数を借用しようとしてしまい、スレッド終了前にローカル変数が破棄されてダングリングポインタになる危険があるためです。
</details>

---

## まとめ

1. **「Rust のクロージャはスタック上の構造体」**: ヒープ確保も GC もなく、手書きの構造体と同じ最高速度で動く。
2. **「FnOnce は消費、FnMut は変更、Fn は読取」**: この 3 兄弟のシグネチャを意識すれば、Rust の借用チェッカーと完全に調和できる。
3. **「別スレッドやスコープ外に渡すなら `move`」**: 所有権をクロージャ内に閉じ込めることでライフタイム問題をクリアする。
